<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/txtSpinnerMemberName"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_margin="4dp"
        android:gravity="center_vertical"
        android:text="Membername"
        android:textSize="20sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/btnSpinnerAction" />

    <ImageButton
        android:id="@+id/btnSpinnerAction"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="right"
        android:layout_margin="4dp"
        android:background="@drawable/icon_cross"
        android:src="@drawable/icon_cross"
        android:visibility="invisible"
        app:layout_constraintLeft_toRightOf="@id/txtSpinnerMemberName"
        app:layout_constraintRight_toRightOf="parent" />

</android.support.constraint.ConstraintLayout>